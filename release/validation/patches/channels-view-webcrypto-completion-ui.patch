diff --git a/frontend/src/views/channels/ChannelsView.test.ts b/frontend/src/views/channels/ChannelsView.test.ts
index 906e09c8e..f9ae2342e 100644
--- a/frontend/src/views/channels/ChannelsView.test.ts
+++ b/frontend/src/views/channels/ChannelsView.test.ts
@@ -341,4 +341,6 @@ describe('ChannelsView messaging behavior', () => {
-    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-
-    expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+      expect((composer.element as HTMLTextAreaElement).value).toBe('')
+    })
+
@@ -369,6 +371,13 @@ describe('ChannelsView messaging behavior', () => {
-    await send.trigger('click')
-    await flushPromises()
-    await send.trigger('click')
-    await flushPromises()
-
-    expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(2)
+    await send.trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+      expect(storedLegacyReplyAttempts()).toHaveLength(1)
+      expect(send.attributes('disabled')).toBeUndefined()
+    })
+    await send.trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(2)
+      expect(storedLegacyReplyAttempts()).toHaveLength(0)
+      expect((composer.element as HTMLTextAreaElement).value).toBe('')
+    })
+
@@ -385,5 +394,6 @@ describe('ChannelsView messaging behavior', () => {
-    await send.trigger('click')
-    await flushPromises()
-    expect(mocks.sendLegacyWhatsAppReply.mock.calls[2]?.[1]?.idempotency_key).toBe(
-      '00000000-0000-4000-8000-000000000012',
-    )
+    await send.trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply.mock.calls[2]?.[1]?.idempotency_key).toBe(
+        '00000000-0000-4000-8000-000000000012',
+      )
+    })
@@ -416,5 +426,5 @@ describe('ChannelsView messaging behavior', () => {
-      await send.trigger('click')
-      await flushPromises()
-
-      expect(storedLegacyReplyAttempts()).toHaveLength(0)
-      expect((composer.element as HTMLTextAreaElement).value).toBe('')
+      await send.trigger('click')
+      await vi.waitFor(() => {
+        expect(storedLegacyReplyAttempts()).toHaveLength(0)
+        expect((composer.element as HTMLTextAreaElement).value).toBe('')
+      })
@@ -423,9 +433,9 @@ describe('ChannelsView messaging behavior', () => {
-      await send.trigger('click')
-      await flushPromises()
-
-      expect(mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key).toBe(
-        '00000000-0000-4000-8000-000000000031',
-      )
-      expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(
-        '00000000-0000-4000-8000-000000000032',
-      )
+      await send.trigger('click')
+      await vi.waitFor(() => {
+        expect(mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key).toBe(
+          '00000000-0000-4000-8000-000000000031',
+        )
+        expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(
+          '00000000-0000-4000-8000-000000000032',
+        )
+      })
@@ -467,3 +477,5 @@ describe('ChannelsView messaging behavior', () => {
-    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-    expect(storedLegacyReplyAttempts()).toHaveLength(1)
+    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+      expect(storedLegacyReplyAttempts()).toHaveLength(1)
+    })
@@ -490,4 +502,4 @@ describe('ChannelsView messaging behavior', () => {
-    await flushPromises()
-
-    expect(storedLegacyReplyAttempts()).toHaveLength(0)
-    expect(view.text()).not.toContain('Settled while switching')
+    await vi.waitFor(() => {
+      expect(storedLegacyReplyAttempts()).toHaveLength(0)
+      expect(view.text()).not.toContain('Settled while switching')
+    })
@@ -502,6 +514,8 @@ describe('ChannelsView messaging behavior', () => {
-    await firstView.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-
-    const firstKey = mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key
-    const stored = storedLegacyReplyAttempts()
-    expect(stored).toHaveLength(1)
+    await firstView.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+      expect(storedLegacyReplyAttempts()).toHaveLength(1)
+    })
+
+    const firstKey = mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key
+    const stored = storedLegacyReplyAttempts()
@@ -522,5 +536,5 @@ describe('ChannelsView messaging behavior', () => {
-    await remounted.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-
-    expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(firstKey)
-    expect(storedLegacyReplyAttempts()).toHaveLength(0)
+    await remounted.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(firstKey)
+      expect(storedLegacyReplyAttempts()).toHaveLength(0)
+    })
@@ -546,5 +560,5 @@ describe('ChannelsView messaging behavior', () => {
-    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-
-    expect(storedLegacyReplyAttempts()).toHaveLength(1)
-    expect((composer.element as HTMLTextAreaElement).value).toBe('Keep this attempt')
+    await view.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(storedLegacyReplyAttempts()).toHaveLength(1)
+      expect((composer.element as HTMLTextAreaElement).value).toBe('Keep this attempt')
+    })
@@ -624,2 +638,5 @@ describe('ChannelsView messaging behavior', () => {
-    await firstView.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
+    await firstView.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply).toHaveBeenCalledTimes(1)
+      expect(storedLegacyReplyAttempts()).toHaveLength(1)
+    })
@@ -633,9 +650,9 @@ describe('ChannelsView messaging behavior', () => {
-    await remounted.get('[data-testid="omnichannel-send-reply"]').trigger('click')
-    await flushPromises()
-
-    expect(mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key).toBe(
-      '00000000-0000-4000-8000-000000000051',
-    )
-    expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(
-      '00000000-0000-4000-8000-000000000052',
-    )
+    await remounted.get('[data-testid="omnichannel-send-reply"]').trigger('click')
+    await vi.waitFor(() => {
+      expect(mocks.sendLegacyWhatsAppReply.mock.calls[0]?.[1]?.idempotency_key).toBe(
+        '00000000-0000-4000-8000-000000000051',
+      )
+      expect(mocks.sendLegacyWhatsAppReply.mock.calls[1]?.[1]?.idempotency_key).toBe(
+        '00000000-0000-4000-8000-000000000052',
+      )
+    })
