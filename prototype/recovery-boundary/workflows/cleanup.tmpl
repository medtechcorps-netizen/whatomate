# Inert design template. It contains no deletion or provider client.
name: Gate-C inert cleanup outline
on: []
permissions:
  contents: read
concurrency:
  group: gate-c-inert-prototype
  cancel-in-progress: false
jobs:
  inert:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Explain inert status
        shell: bash
        run: |
          printf '%s\n' 'gate-c-nonproduction-only inert template'
